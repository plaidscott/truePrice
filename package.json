{
  "name": "trueprice",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^3.5.0",
    "react-scripts": "1.0.10"
  },
  "dependencies": {
    "concurrently": "^3.5.0",
    "json-server": "^0.11.0",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "yarn": "^0.27.5"
  },
  "scripts": {
    "start-both": "concurrently \"npm run start\" \"json-server --watch db.json --port 3001\" ",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
