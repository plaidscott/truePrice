import React, { Component } from 'react';

import "../styles/VehicleInput.css"


class VehicleInput extends Component {
    render() {
        return (
            <div className="VehicleInputMainStyle">
               <p>There is stuff in the VehicleInput</p>
                <p>But just how much stuff is in this section, and how much of it has to do about cheese? Cheese being one of the major food groups.</p>
            </div>
        );
    }
}

export default VehicleInput;